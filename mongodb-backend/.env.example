MONGO_URI=mongodb://127.0.0.1:27017
DB_NAME=malsync
PORT=8787
# Optional. If set, only API keys whose hash matches a row in apiKeys are accepted.
# When unset and ALLOW_ANON=1, a single "anonymous" owner is used (localhost dev only).
ALLOW_ANON=0
